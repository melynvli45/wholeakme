function inspect(url){let s=0,u=url.toLowerCase();['login','verify','secure','bonus','gift','update','account','bank','pay'].forEach(w=>{if(u.includes(w))s+=1});if(u.startsWith('http://'))s+=1;if(/\d+\.\d+\.\d+\.\d+/.test(u))s+=2;return s>=3?{level:'High',score:70,message:'Multiple phishing-style indicators detected.'}:s>0?{level:'Medium',score:40,message:'Some suspicious URL patterns detected.'}:{level:'Low',score:10,message:'No high-risk local indicator detected.'}}
chrome.tabs.onUpdated.addListener((tabId,info,tab)=>{if(info.status==='complete'&&tab.url&&/^https?:/.test(tab.url)){const r=inspect(tab.url);chrome.storage.local.set({lastScan:{url:tab.url,...r,time:new Date().toISOString()}});if(r.score>=40)chrome.notifications.create({type:'basic',iconUrl:'icon128.png',title:'WhoLeakMe Warning',message:r.message});}});
