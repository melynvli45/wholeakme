chrome.storage.local.get('lastScan',({lastScan})=>{if(lastScan&&lastScan.url===location.href&&lastScan.score>=40){const b=document.createElement('div');b.textContent='⚠ WhoLeakMe: '+lastScan.level+' risk — '+lastScan.message;b.style.cssText='position:fixed;top:12px;right:12px;z-index:2147483647;background:#7f1d1d;color:#fff;padding:12px 16px;border-radius:10px;font:14px Arial;box-shadow:0 8px 24px #0005';document.documentElement.appendChild(b);setTimeout(()=>b.remove(),8000);}});
